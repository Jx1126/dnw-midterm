<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/output.css" />
    <title>BLOG-IT: Author Settings</title>
  </head>
  <body>
    <!-- Header Navbar -->
    <%- include('../views/layout/header.ejs') %>

    <!-- Links to author homepage -->
    <div class="flex ml-10 mt-5 ">
      <form action="/author/home" method="GET">
        <button href="#" class="btn btn-secondary w-40">
          <!-- Return icon -->
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6">
            <path fill-rule="evenodd" d="M9.53 2.47a.75.75 0 0 1 0 1.06L4.81 8.25H15a6.75 6.75 0 0 1 0 13.5h-3a.75.75 0 0 1 0-1.5h3a5.25 5.25 0 1 0 0-10.5H4.81l4.72 4.72a.75.75 0 1 1-1.06 1.06l-6-6a.75.75 0 0 1 0-1.06l6-6a.75.75 0 0 1 1.06 0Z" clip-rule="evenodd" />
          </svg>
          Return
        </button>
      </form>
    </div>

    <!-- Hero section -->
    <section>
      <h1>Author Settings</h1>
    </section>
    
    
    <!-- Author Settings Form -->
    <section>
      <!-- Display input error messages -->
      <div class="flex flex-col items-center justify-center gap-2">
        <!-- Loop through the error object to display the errors in the input -->
        <% if(typeof alert != 'undefined') { %>
          <% alert.forEach(function(error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative max-w-xl w-full" role="alert">
              <strong class="font-bold">Invalid Input:</strong>
              <span class="block sm:inline">
                <%= error.msg %>
              </span>
            </div>
          <% }) %>
        <% } %>
      </div>
      <div class="flex justify-center align-center">
        <div class="mt-7 gap-5 p-5 overflow-hidden bg-slate-400 shadow-xl rounded-lg w-auto">
          <div class="flex flex-col p-5 pl-7 bg-slate-200 rounded-lg shadow-lg">
            <form action="/author/settings" method="POST">
              <label class="form-control w-full max-w-xs">
                <div class="label">
                  <span class="label-text text-slate-500 font-semibold">Author name?</span>
                </div>
                <input type="text" placeholder="Name" autocomplete="off" class="input input-bordered w-full max-w-xs" name="author_name" id="author_name" value="<%= typeof(author) != 'undefined' ? author.author_name : '' %>" />
              </label>
      
              <label class="form-control w-full max-w-xs">
                <div class="label">
                  <span class="label-text text-slate-500 font-semibold">Blog title?</span>
                </div>
                <input type="text" placeholder="Title" autocomplete="off" class="input input-bordered w-full max-w-xs" name="blog_title" id="blog_title" value="<%= typeof(author) != 'undefined' ? author.blog_title : '' %>" />
              </label>
              <button type="submit" class="btn btn-primary btn-wide mt-5">Submit</button>
            </form>
          </div>
        </div>
      </div>
    </section>

    
    <!-- Footer -->
    <%- include('../views/layout/footer.ejs') %>
  </body>
</html>